import React from 'react';
import MainContent from '../MainContent';
import Voting from "../Pages/Voting"

const VotingPage=()=>{
    return <MainContent content={<Voting />} />;
}

export default VotingPage;